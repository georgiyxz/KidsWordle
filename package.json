{
  "name": "word-garden",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "optimize-assets": "node scripts/optimize-assets.mjs",
    "test": "node scripts/test-scoreGuess.ts"
  },
  "dependencies": {
    "phaser": "^3.80.0"
  },
  "devDependencies": {
    "sharp": "^0.33.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
